import { Typography } from '@mui/material';
import React from 'react';

const NothingSelected = () => {
  return <Typography>Start typing to search...</Typography>;
};

export default NothingSelected;
